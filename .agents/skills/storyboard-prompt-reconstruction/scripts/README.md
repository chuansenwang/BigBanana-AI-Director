# Scripts

Deterministic helpers for prompt reconstruction from `storyboard-timecode-slicing` outputs:

- `build_analysis_scaffold.py` — read upstream `manifest.json` and emit a stable prompt-analysis JSON scaffold
- `build_prompt_csv.py` — validate the completed analysis JSON and flatten it into CSV
