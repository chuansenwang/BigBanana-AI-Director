
- Decision after Task 1: treat `components/Dashboard.tsx` as the source of truth for current homepage behavior, even where the plan’s baseline language appears stale.
- Decision after Task 1: preserve existing direct `navigate(...)` behavior for project/account flows unless a later task proves callback wiring is required.
