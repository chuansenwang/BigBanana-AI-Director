
- TikTok public links can still fail with 403 or 429 responses due to anti-bot or rate-limit behavior, so the skill now tells the agent to stop and report V1 scope limits instead of inventing cookie-based workarounds.
