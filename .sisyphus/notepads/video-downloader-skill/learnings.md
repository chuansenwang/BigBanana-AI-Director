
- Followed the local skill pattern: YAML frontmatter first, then direct markdown sections with trigger guidance and copy-pasteable commands.
- Locked V1 scope to public YouTube and TikTok video URLs, single URL, batch UTF-8 URL file, custom filename template, metadata-only mode, and audio-only export.
