
- Chose `yt-dlp` plus FFmpeg because the task required a Windows-first downloader workflow with stable metadata, download, muxing, and audio extraction commands.
- Preferred standalone `yt-dlp.exe` and FFmpeg on `PATH` because that matches the requested Windows-first setup and avoids making Python the primary dependency path.
